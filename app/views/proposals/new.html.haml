= semantic_form_for @content, :as => :content, :url => user_contents_url(@content_type.slug), :html => { :multipart => true } do |form|
  = render 'form', :f => form
  = render 'admin/shared/form_actions', :back_url => "/", :button_label => :create

